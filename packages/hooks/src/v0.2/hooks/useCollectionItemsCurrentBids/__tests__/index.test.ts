import { useCollectionItemsCurrentBids } from '../useCollectionItemsCurrentBids';

describe('useCollectionItemsCurrentBids', () => {
  it('should be defined', () => {
    expect(useCollectionItemsCurrentBids).toBeDefined();
  });
});
